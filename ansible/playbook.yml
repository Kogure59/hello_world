---
- hosts: target_node
  become: yes
  roles:
    # - { role: yum, tags: yum }
    # - { role: nvm, tags: nvm }
    # - { role: yarn, tags: yarn }
    - { role: ruby, tags: ruby }
    - { role: rails, tags: rails }
    # - { role: bundler, tags: bundler }
    # - { role: mysql, tags: mysql }
    # - { role: app, tags: app }
    # - { role: nginx, tags: nginx }   