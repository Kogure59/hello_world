---
- name: Check if mariadb-libs is installed
  yum:
    name: mariadb-libs
    state: present
  register: mariadb_libs_installed
  ignore_errors: yes

- name: Remove mariadb-libs
  yum:
    name: mariadb-libs
    state: absent
  when: mariadb_libs_installed is succeeded

- name: Check if MySQL 8.0 community release RPM is already installed
  command: rpm -q mysql80-community-release-el7-9
  register: mysql_repo_check
  ignore_errors: yes

- name: Download MySQL 8.0 community release RPM
  get_url:
    url: https://dev.mysql.com/get/mysql80-community-release-el7-9.noarch.rpm
    dest: /tmp/mysql80-community-release-el7-9.noarch.rpm
  when: mysql_repo_check.failed

- name: Install MySQL 8.0 community release RPM
  yum:
    name: /tmp/mysql80-community-release-el7-9.noarch.rpm
    state: present
  when: mysql_repo_check.failed
  register: mysql_repo_installed

- name: Enable mysql80-community
  command: |
    yum-config-manager --disable mysql57-community
    yum-config-manager --enable mysql80-community
  when: mysql_repo_installed.changed

- name: install mysql-community-client
  yum:
    name: mysql-community-client
    state: present
  when: mysql_repo_installed.changed

- name: Install MySQL development tools
  yum:
    name: mysql-community-devel
    state: present
  when: mysql_repo_installed.changed